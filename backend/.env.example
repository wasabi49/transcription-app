# Transcription App Backend - 環境変数設定
# このファイルをコピーして .env を作成してください

# デバッグモード
DEBUG=false

# CORS許可オリジン（カンマ区切り）
CORS_ORIGINS=["http://localhost:3000"]

# アップロード制限（バイト単位、デフォルト50MB）
MAX_FILE_SIZE=52428800

# レート制限（slowapi形式）
RATE_LIMIT=3/minute

# 同時採譜処理数
MAX_CONCURRENT_TRANSCRIPTIONS=1
